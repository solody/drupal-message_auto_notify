{#
/**
 * @file user_notify_setting.html.twig
 * Default theme implementation to present User notify setting data.
 *
 * This template is used when viewing User notify setting pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_user_notify_setting()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('user_notify_setting') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
