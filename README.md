# Message Auto Notify

This module provides a way to automatically send messages to users when certain
message is created.

Entity types:
- Notification: Configure which notifier to use for each message type.
- UserNotifySetting: Message sending strategy of every user.
